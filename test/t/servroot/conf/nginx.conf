worker_processes  1;
daemon on;
master_process off;
error_log /home/p/projects/nginx/postgres/module/test/t/servroot/logs/error.log debug;
pid       /home/p/projects/nginx/postgres/module/test/t/servroot/logs/nginx.pid;

http {
    access_log /home/p/projects/nginx/postgres/module/test/t/servroot/logs/access.log;

    default_type text/plain;
    keepalive_timeout  68;

        upstream database {
        postgres_server     127.0.0.1 dbname=test user=monty password=some_pass;
    }


    server {
        listen          1984;
        server_name     localhost;

        client_max_body_size 30M;
        #client_body_buffer_size 4k;

        # Begin preamble config...

        # End preamble config...

        # Begin test case config...
    location /postgres {
        set                 $backend  database;
        postgres_pass       $backend;
        postgres_query      "update cats set name='bob' where name='bob'";
    }

        # End test case config.

        location / {
            root /home/p/projects/nginx/postgres/module/test/t/servroot/html;
            index index.html index.htm;
        }
    }
}

events {
    worker_connections  128;
}

